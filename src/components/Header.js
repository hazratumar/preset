import React from 'react'
import { Button, Menu, Layout } from 'antd'
const { Header } = Layout;

const Headers = () => {
  return (
    <Header>
      Header
    </Header>
  )
}

export default Headers